# Botng - نظام الأتمتة الذكي

> شركة ليفل أب القابضة

نظام أتمتة ذكي يجمع البيانات من مصادر متعددة، يحللها بـ OpenAI، ويرسل تقارير عبر واتساب.

## 🚀 المميزات

- 📊 تقارير يومية وأسبوعية تلقائية
- 🤖 تحليل ذكي بالـ AI (OpenAI)
- 📱 تواصل عبر واتساب (Ultra Message)
- 📈 جمع بيانات من Clarity والكول سنتر
- ⏰ جدولة تقارير تلقائية

## 📋 المتطلبات

- Python 3.11+
- PostgreSQL 15+
- Docker & Docker Compose (اختياري)

## ⚙️ التثبيت

### 1. نسخ الملفات

```bash
git clone <repo-url>
cd botng
```

### 2. إعداد البيئة

```bash
cp .env.example .env
# عدّل ملف .env بالقيم الصحيحة
```

### 3. التشغيل بـ Docker

```bash
docker-compose up -d
```

### أو التشغيل محلياً

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## 🔧 الإعدادات

```env
# Database
DATABASE_URL=postgresql://user:pass@localhost/botng

# OpenAI
OPENAI_API_KEY=sk-xxx

# Ultra Message
ULTRAMSG_INSTANCE_ID=xxx
ULTRAMSG_TOKEN=xxx
ULTRAMSG_PHONE=966xxxxxxxxx

# App
ADMIN_PHONE=966xxxxxxxxx
REPORT_TIME=08:00
```

## 📡 API Endpoints

| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/health` | فحص حالة النظام |
| GET | `/api/reports/daily` | التقرير اليومي |
| GET | `/api/reports/weekly` | التقرير الأسبوعي |
| POST | `/api/reports/send` | إرسال تقرير |
| POST | `/api/data/sync` | مزامنة البيانات |
| POST | `/webhook/whatsapp` | استقبال رسائل واتساب |

## 💬 أوامر واتساب

| الأمر | الوظيفة |
|-------|---------|
| `تقرير` | تقرير اليوم الكامل |
| `كول سنتر` | تقرير المكالمات |
| `زوار` | تقرير Clarity |
| `مقارنة` | مقارنة بالأمس |
| `مساعدة` | قائمة الأوامر |

## 📁 هيكل المشروع

```
botng/
├── app/
│   ├── main.py              # FastAPI app
│   ├── config.py            # Environment variables
│   ├── database.py          # DB connection & models
│   │
│   ├── collectors/          # جمع البيانات
│   │   ├── clarity.py
│   │   └── callcenter.py
│   │
│   ├── analyzers/           # التحليل
│   │   └── openai_analyzer.py
│   │
│   ├── reporters/           # إنشاء التقارير
│   │   └── report_generator.py
│   │
│   ├── integrations/        # الربط الخارجي
│   │   └── ultramsg.py
│   │
│   ├── webhooks/            # استقبال رسائل واتساب
│   │   └── whatsapp.py
│   │
│   └── scheduler/           # جدولة التقارير
│       └── jobs.py
│
├── requirements.txt
├── .env.example
├── Dockerfile
└── docker-compose.yml
```

## 🔒 الأمان

- لا تشارك ملف `.env` أبداً
- استخدم HTTPS في الإنتاج
- فعّل Webhook verification

## 📞 الدعم

شركة ليفل أب القابضة
